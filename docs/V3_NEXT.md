# V3 next stage

After database CI is green, the next implementation stage is the dashboard shell and realtime operational data path:

- React + TypeScript + Vite + Tailwind dashboard
- authenticated session bootstrap
- instance list/detail views
- lifecycle controls
- WebSocket connection and instance subscriptions
- TanStack Query cache synchronization
- mobile-first operational layout

Scanner, task controls, command management, and integrations remain behind the existing V3 service boundaries.
